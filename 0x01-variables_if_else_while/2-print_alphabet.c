#include <stdlib.h>
#include <stdio.h>

/**
 *      main - Will print the alphabet in lower case.
 *
 *      Return: 0 (Success)
 */

int main(void)
{
	char l;

	for (l = 'a'; l <= 'z'; l++)
	putchar(l);
	putchar('\n');
	return (0);
}
