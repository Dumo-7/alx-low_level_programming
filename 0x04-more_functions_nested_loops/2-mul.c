#include "main.h"

/**
 * mul - miltiplies two integers
 * @a: integer to be multiplied
 * @b: integer to be multiplied
 *
 * Return: 0 is successful
 */

int mul(int a, int b)
{
	if (a % 10 == 0)
	{
		if (b % 10 == 0)
		{
		(a * b);
		}
	}
}
