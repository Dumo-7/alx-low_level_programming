#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @int: integer to be checked
 *
 * Return: Always 0.
 *
 */

int _abs(int)
{
	int n;

	if (n < 0)
	{
		(n * -1);
	}
	else if (n > 0)
	{
		(n * 1);
	}
	return (0);

}
